import { CommandController } from "./CommandController";

export type CommandRoutes = ReturnType<typeof CommandRoutes>;
export const CommandRoutes = (commandController: CommandController) => {
    return;
};